<% if current_user %>
	<div id="newMessage">
	  <h2>New message</h2>
	  <%= render 'form' %>
	  <%= link_to 'Back', messages_path %>
	</div>
<% else %>
    <% flash[:error] = "Unauthorized - The page is not available." %>
<%end%>