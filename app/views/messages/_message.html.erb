<% if message.recepient_id == @user.id %>
<table class="table">
  <tr>
    <td>
      <%= link_to message.subject, message_path(message) %>
    </td>
    <td>
      <%= message.sender_id %>
    </td>
    <td>
      <%= message.created_at %>
    </td>
    <td><%= link_to 'Delete Message', message, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
</table>
<% end %>