<%= form_for([@article, @article.comments.new], remote: true) do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>

	<%= f.label :email %>
	<%= f.text_field :email %>

	<%= f.label :body %>
	<%= f.text_area :body %>

	<%= f.submit "Add", class: "btn btn-large" %>
<% end %>